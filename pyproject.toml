[project]
name = "foodscraps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "contextily>=1.6.0",
    "geodatasets>=2026.1.0",
    "geopandas>=1.1.3",
    "google-auth>=2.48.0",
    "google-maps-areainsights>=0.3.0",
    "ipykernel>=7.2.0",
    "kaleido>=1.2.0",
    "matplotlib>=3.10.8",
    "nbformat>=5.10.4",
    "node>=2.0.0",
    "openai>=3.7.0",
    "pandas>=3.0.1",
    "plotly>=6.7",
    "pre-commit>=4.5.1",
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
    "scikit-learn>=1.8.0",
    "seaborn>=0.13.2",
    "selenium>=4.43.0",
    "streamlit>=1.56.0",
    "timer-for-python>=0.9.9",
    "tqdm>=4.70.0",
    "watchdog>=6.0.0",
    "webdriver-manager>=4.0.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/foodscraper"]

[tool.ruff.lint.per-file-ignores]
"*.ipynb" = ["F403", "F405"]
